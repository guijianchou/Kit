<local:NavigablePage
    x:Class="Kit.Settings.UI.Views.GeneralPage"
    NavigationCacheMode="Required"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Kit.Settings.UI.Controls"
    xmlns:converters="using:Kit.Settings.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Kit.Settings.UI.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Kit.AiHub.Models"
    xmlns:ptcontrols="using:Kit.Common.UI.Controls"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:viewModels="using:Kit.Settings.UI.ViewModels"
    AutomationProperties.LandmarkType="Main"
    mc:Ignorable="d">

    <local:NavigablePage.Resources>
        <converters:StringToInfoBarSeverityConverter x:Key="StringToInfoBarSeverityConverter" />
    </local:NavigablePage.Resources>

    <controls:SettingsPageControl x:Uid="General" ModuleImageSource="ms-appx:///Assets/Settings/Modules/PT.png">
        <controls:SettingsPageControl.ModuleContent>
            <StackPanel ChildrenTransitions="{StaticResource SettingsCardsAnimations}" Orientation="Vertical">
                <controls:SettingsGroup x:Uid="General_VersionAndUpdate" Margin="0,-32,0,0">
                    <tkcontrols:SettingsExpander Header="{x:Bind ViewModel.PowerToysVersion, Mode=OneWay}" HeaderIcon="{ui:FontIcon Glyph=&#xE895;}">
                        <tkcontrols:SettingsExpander.Description>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Style="{StaticResource SecondaryTextStyle}">
                                    <Run x:Uid="General_VersionLastChecked" />
                                    <Run Text="{x:Bind ViewModel.UpdateCheckedDate, Mode=OneWay}" />
                                </TextBlock>
                                <HyperlinkButton
                                    x:Uid="ReleaseNotes"
                                    Margin="0,2,0,0"
                                    FontWeight="SemiBold"
                                    NavigateUri="https://github.com/guijianchou/Kit/releases/" />
                            </StackPanel>
                        </tkcontrols:SettingsExpander.Description>
                        <Grid Visibility="{x:Bind ViewModel.IsUpdatePanelVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="18"
                                Visibility="{x:Bind ViewModel.IsCheckingForUpdates, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                                <ProgressRing Width="24" Height="24" />
                                <TextBlock
                                    x:Uid="General_CheckingForUpdates"
                                    VerticalAlignment="Center"
                                    FontWeight="SemiBold"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <Button
                                x:Uid="GeneralPage_CheckForUpdates"
                                HorizontalAlignment="Right"
                                Command="{Binding CheckForUpdatesEventHandler}"
                                IsEnabled="{x:Bind ViewModel.IsCheckForUpdatesButtonEnabled, Mode=OneWay}"
                                Visibility="{Binding Mode=OneWay, Path=IsCheckingForUpdates, Converter={StaticResource BoolNegationConverter}}" />
                        </Grid>
                    </tkcontrols:SettingsExpander>

                    <StackPanel Orientation="Vertical">
                        <InfoBar
                            Title="{x:Bind ViewModel.UpdateCheckMessage, Mode=OneWay}"
                            IsClosable="False"
                            IsOpen="{x:Bind ViewModel.UpdateCheckMessageVisible, Mode=OneWay}"
                            IsTabStop="{x:Bind ViewModel.UpdateCheckMessageVisible, Mode=OneWay}"
                            Severity="{x:Bind ViewModel.UpdateCheckMessageSeverity, Converter={StaticResource StringToInfoBarSeverityConverter}, Mode=OneWay}">
                            <InfoBar.ActionButton>
                                <HyperlinkButton
                                    x:Uid="GeneralPage_ViewRelease"
                                    NavigateUri="{x:Bind ViewModel.PowerToysNewAvailableVersionUri, Mode=OneWay}"
                                    Visibility="{x:Bind ViewModel.IsUpdateAvailable, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                            </InfoBar.ActionButton>
                        </InfoBar>
                    </StackPanel>
                </controls:SettingsGroup>

                <controls:SettingsGroup x:Uid="StartupAndPermissions">
                    <tkcontrols:SettingsCard
                        x:Name="GeneralPageRunAtStartUp"
                        x:Uid="GeneralPage_RunAtStartUp"
                        HeaderIcon="{ui:FontIcon Glyph=&#xF71C;}"
                        IsEnabled="{x:Bind ViewModel.IsRunAtStartupGPOManaged, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}">
                        <ToggleSwitch
                            x:Uid="ToggleSwitch"
                            AutomationProperties.Name="{Binding ElementName=GeneralPageRunAtStartUp, Path=Header}"
                            IsOn="{x:Bind ViewModel.Startup, Mode=TwoWay}" />
                    </tkcontrols:SettingsCard>
                    <tkcontrols:SettingsExpander
                        x:Name="AdminModeRunningAs"
                        x:Uid="Admin_Mode_Running_As"
                        Header="{x:Bind ViewModel.RunningAsText, Mode=OneWay}"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE7EF;}"
                        IsExpanded="True">
                        <tkcontrols:SettingsExpander.Description>
                            <HyperlinkButton x:Uid="GeneralPage_ToggleSwitch_AlwaysRunElevated_Link" NavigateUri="https://github.com/guijianchou/Kit" />
                        </tkcontrols:SettingsExpander.Description>
                        <Button
                            x:Uid="GeneralPage_RestartAsAdmin_Button"
                            Command="{Binding RestartElevatedButtonEventHandler}"
                            IsEnabled="{Binding IsAdminButtonEnabled}" />
                        <tkcontrols:SettingsExpander.Items>
                            <tkcontrols:SettingsCard ContentAlignment="Left" IsEnabled="{x:Bind ViewModel.IsElevated, Mode=OneWay}">
                                <ptcontrols:CheckBoxWithDescriptionControl x:Uid="GeneralSettings_AlwaysRunAsAdminText" IsChecked="{x:Bind ViewModel.RunElevated, Mode=TwoWay}" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard ContentAlignment="Left">
                                <CheckBox x:Uid="GeneralPage_WarningsElevatedApps" IsChecked="{Binding Mode=TwoWay, Path=EnableWarningsElevatedApps}" />
                            </tkcontrols:SettingsCard>
                        </tkcontrols:SettingsExpander.Items>
                    </tkcontrols:SettingsExpander>
                </controls:SettingsGroup>

                <controls:SettingsGroup x:Uid="Appearance_Behavior" IsEnabled="True">
                    <tkcontrols:SettingsCard
                        x:Name="LanguageHeader"
                        x:Uid="LanguageHeader"
                        HeaderIcon="{ui:FontIcon Glyph=&#xF2B7;}">
                        <ComboBox
                            x:Name="Languages_ComboBox"
                            MinWidth="{StaticResource SettingActionControlMinWidth}"
                            AutomationProperties.Name="{Binding ElementName=LanguageHeader, Path=Header}"
                            DisplayMemberPath="Language"
                            ItemsSource="{Binding Languages, Mode=TwoWay}"
                            SelectedIndex="{Binding LanguagesIndex, Mode=TwoWay}" />
                    </tkcontrols:SettingsCard>
                    <InfoBar
                        x:Uid="LanguageRestartInfo"
                        IsClosable="False"
                        IsOpen="{x:Bind Mode=OneWay, Path=ViewModel.LanguageChanged}"
                        IsTabStop="True"
                        Severity="Informational">
                        <InfoBar.ActionButton>
                            <Button x:Uid="LanguageRestartInfoButton" Click="Click_LanguageRestart" />
                        </InfoBar.ActionButton>
                    </InfoBar>

                    <tkcontrols:SettingsCard
                        x:Name="ColorModeHeader"
                        x:Uid="ColorModeHeader"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE790;}">
                        <tkcontrols:SettingsCard.Description>
                            <HyperlinkButton x:Uid="Windows_Color_Settings" Click="OpenColorsSettings_Click" />
                        </tkcontrols:SettingsCard.Description>
                        <ComboBox
                            MinWidth="{StaticResource SettingActionControlMinWidth}"
                            AutomationProperties.Name="{Binding ElementName=ColorModeHeader, Path=Header}"
                            SelectedIndex="{x:Bind ViewModel.ThemeIndex, Mode=TwoWay}">
                            <ComboBoxItem x:Uid="Radio_Theme_Dark" />
                            <ComboBoxItem x:Uid="Radio_Theme_Light" />
                            <ComboBoxItem x:Uid="Radio_Theme_Default" />
                        </ComboBox>
                    </tkcontrols:SettingsCard>

                    <controls:GPOInfoControl ShowWarning="{x:Bind ViewModel.IsRunAtStartupGPOManaged, Mode=OneWay}">
                        <tkcontrols:SettingsExpander
                            x:Name="ShowSystemTrayIconCard"
                            x:Uid="ShowSystemTrayIcon"
                            HeaderIcon="{ui:FontIcon Glyph=&#xE75B;}">
                            <ToggleSwitch
                                x:Uid="ToggleSwitch"
                                AutomationProperties.Name="{Binding ElementName=ShowSystemTrayIconCard, Path=Header}"
                                IsOn="{x:Bind ViewModel.ShowSysTrayIcon, Mode=TwoWay}"
                                Toggled="ShowSystemTrayIcon_Toggled" />
                            <tkcontrols:SettingsExpander.Items>
                                <tkcontrols:SettingsCard
                                    x:Name="ShowThemeAdaptiveTrayIconCard"
                                    ContentAlignment="Left"
                                    IsEnabled="{x:Bind ViewModel.ShowSysTrayIcon, Mode=OneWay}">
                                    <CheckBox x:Uid="ShowThemeAdaptiveTrayIcon" IsChecked="{x:Bind ViewModel.ShowThemeAdaptiveTrayIcon, Mode=TwoWay}" />
                                </tkcontrols:SettingsCard>
                            </tkcontrols:SettingsExpander.Items>
                        </tkcontrols:SettingsExpander>
                    </controls:GPOInfoControl>

                    <tkcontrols:SettingsExpander
                        x:Name="GeneralPageEnableQuickAccess"
                        x:Uid="GeneralPage_EnableQuickAccess"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE91C;}"
                        IsExpanded="True">
                        <ToggleSwitch
                            x:Uid="ToggleSwitch"
                            AutomationProperties.Name="{Binding ElementName=GeneralPageEnableQuickAccess, Path=Header}"
                            IsOn="{x:Bind ViewModel.EnableQuickAccess, Mode=TwoWay}" />
                        <tkcontrols:SettingsExpander.Items>
                            <!--  HACK: For some weird reason, a ShortcutControl does not work correctly if it's the first or last item in the expander, so we add an invisible card.  -->
                            <tkcontrols:SettingsCard Visibility="Collapsed" />
                            <tkcontrols:SettingsCard
                                x:Name="QuickAccessShortcut"
                                x:Uid="GeneralPage_QuickAccessShortcut"
                                IsEnabled="{x:Bind ViewModel.EnableQuickAccess, Mode=OneWay}">
                                <controls:ShortcutControl HotkeySettings="{x:Bind ViewModel.QuickAccessShortcut, Mode=TwoWay}" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard Visibility="Collapsed" />
                        </tkcontrols:SettingsExpander.Items>
                    </tkcontrols:SettingsExpander>
                </controls:SettingsGroup>


                <!-- Diagnostics & Logging Group -->
                <controls:SettingsGroup x:Uid="General_DiagnosticsGroup">
                    <tkcontrols:SettingsExpander
                        x:Name="GeneralDiagnosticsExpander"
                        x:Uid="General_DiagnosticsExpander"
                        HeaderIcon="{ui:FontIcon Glyph=&#xEC19;}"
                        IsExpanded="True">
                        <ToggleSwitch
                            x:Uid="ToggleSwitch"
                            AutomationProperties.Name="{Binding ElementName=GeneralDiagnosticsExpander, Path=Header}"
                            IsOn="{x:Bind ViewModel.EnableLogging, Mode=TwoWay}" />
                        <tkcontrols:SettingsExpander.Items>
                            <tkcontrols:SettingsCard
                                x:Uid="General_LogLevelCard"
                                IsEnabled="{x:Bind ViewModel.EnableLogging, Mode=OneWay}">
                                <ComboBox
                                    MinWidth="{StaticResource SettingActionControlMinWidth}"
                                    SelectedIndex="{x:Bind ViewModel.LogLevelIndex, Mode=TwoWay}">
                                    <ComboBoxItem x:Uid="General_LogLevelTrace" Content="Trace (Verbose)" Tag="trace" />
                                    <ComboBoxItem x:Uid="General_LogLevelDebug" Content="Debug" Tag="debug" />
                                    <ComboBoxItem x:Uid="General_LogLevelInfo" Content="Info" Tag="info" />
                                    <ComboBoxItem x:Uid="General_LogLevelWarning" Content="Warning" Tag="warn" />
                                    <ComboBoxItem x:Uid="General_LogLevelError" Content="Error" Tag="err" />
                                </ComboBox>
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard
                                x:Uid="General_OpenLogsFolderCard"
                                Description="{x:Bind ViewModel.LogsFolderPath, Mode=OneWay}">
                                <Button
                                    x:Uid="General_OpenLogsFolderButton"
                                    Command="{x:Bind ViewModel.OpenLogsFolderCommand}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xEC50;" FontSize="13" />
                                        <TextBlock x:Uid="General_OpenLogsFolderText" Text="Open folder" />
                                    </StackPanel>
                                </Button>
                            </tkcontrols:SettingsCard>
                        </tkcontrols:SettingsExpander.Items>
                    </tkcontrols:SettingsExpander>
                </controls:SettingsGroup>


                <!-- AI Service. Shared by every module: the execution kernel, endpoints,
                     credentials and the global security policy that applies to all plugins.
                     Moved here from the AI Hub page so it reads as a base capability. -->
                <controls:SettingsGroup x:Uid="General_AiServicesGroup" Visibility="Visible">
                <tkcontrols:SettingsCard
                    x:Name="GeneralPageAiHubMasterCard"
                    x:Uid="General_AiHubSettings"
                    AutomationProperties.AutomationId="AiHubSettingsCard"
                    HeaderIcon="{ui:FontIcon Glyph=&#xEA80;}">
                    <ToggleSwitch
                        x:Uid="AiHub_EnableToggle"
                        AutomationProperties.AutomationId="Toggle_AiHub"
                        AutomationProperties.Name="AI Hub"
                        IsOn="{x:Bind ViewModel.AiServices.IsEnabled, Mode=TwoWay}"
                        IsEnabled="{x:Bind ViewModel.AiServices.CanToggle, Mode=OneWay}" />
                </tkcontrols:SettingsCard>
    
                <!-- Compact configuration revealed only when AI Hub is enabled -->
                    <!-- Dims the service configuration while it is switched off. -->
                    <ContentControl
                        IsEnabled="{x:Bind ViewModel.AiServices.IsEnabled, Mode=OneWay}"
                        Opacity="{x:Bind ViewModel.AiServices.IsEnabled, Mode=OneWay, Converter={StaticResource BoolToDisabledOpacityConverter}}"
                        IsTabStop="False"
                        HorizontalContentAlignment="Stretch">
                <StackPanel Spacing="8" Margin="0,4,0,0"
                            Visibility="{x:Bind ViewModel.AiServices.HubContentVisibility, Mode=OneWay}">
    
                    <InfoBar
                        IsOpen="{x:Bind ViewModel.AiServices.IsStatusOpen, Mode=TwoWay}"
                        Message="{x:Bind ViewModel.AiServices.StatusMessage, Mode=OneWay}"
                        Severity="{x:Bind ViewModel.AiServices.StatusSeverity, Mode=OneWay}"
                        IsClosable="True" />
    
                    <!-- 1. AI execution kernel (SettingsExpander) -->
                    <tkcontrols:SettingsExpander
                        x:Name="AiKernelSettingsExpander"
                        x:Uid="AiHub_KernelCard"
                        Description="{x:Bind ViewModel.AiServices.KernelStatusText, Mode=OneWay}"
                        HeaderIcon="{ui:FontIcon Glyph=&#xEDA3;}"
                        IsExpanded="False">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <ComboBox Width="160"
                                      SelectedIndex="{x:Bind ViewModel.AiServices.PendingKernelIndex, Mode=TwoWay}"
                                      IsEnabled="{x:Bind ViewModel.AiServices.CanEdit, Mode=OneWay}">
                                <ComboBoxItem x:Uid="AIHub_KernelCodexItem" Content="Codex CLI" />
                                <ComboBoxItem x:Uid="AIHub_KernelPiItem" Content="Pi CLI" />
                            </ComboBox>
                            <Button x:Uid="AiHub_ApplyKernelButton"
                                    Content="Apply"
                                    Command="{x:Bind ViewModel.AiServices.ApplyKernelSwitchCommand}"
                                    Visibility="{x:Bind ViewModel.AiServices.IsKernelSwitchPending, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                    Style="{StaticResource AccentButtonStyle}" />
                        </StackPanel>
                        <tkcontrols:SettingsExpander.Items>
                            <tkcontrols:SettingsCard x:Uid="AiHub_KernelUpdatesCard" Header="Kernel updates" Description="{x:Bind ViewModel.AiServices.KernelLatestText, Mode=OneWay}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button Command="{x:Bind ViewModel.AiServices.CheckKernelUpdatesCommand}">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon Glyph="&#xE72C;" FontSize="13" />
                                            <TextBlock x:Uid="AiHub_CheckUpdatesText" Text="Check for updates" />
                                        </StackPanel>
                                    </Button>
                                    <Button Command="{x:Bind ViewModel.AiServices.DownloadKernelCommand}" Style="{StaticResource AccentButtonStyle}">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon Glyph="&#xE896;" FontSize="13" />
                                            <TextBlock x:Uid="AiHub_DownloadKernelText" Text="Download or update" />
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard Visibility="{x:Bind ViewModel.AiServices.ShowDownloadProgress, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <ProgressBar Minimum="0" Maximum="1" Value="{x:Bind ViewModel.AiServices.DownloadProgress, Mode=OneWay}" HorizontalAlignment="Stretch" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard x:Uid="AiHub_KernelLocationCard" Header="Installation path">
                                <TextBlock Text="{x:Bind ViewModel.AiServices.KernelPathText, Mode=OneWay}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           IsTextSelectionEnabled="True"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </tkcontrols:SettingsCard>
                        </tkcontrols:SettingsExpander.Items>
                    </tkcontrols:SettingsExpander>

                    <!-- Service self-test. Distinguishes a broken service from a broken
                         plugin: if this passes, the engine, policy, kernel and validation
                         all work, so the problem lies in the calling plugin. -->
                    <tkcontrols:SettingsCard x:Uid="AiHub_SelfTestCard" Header="Service self-test"
                                             Description="Runs a probe through the whole pipeline to verify the service independently of any plugin.">
                        <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                            <ProgressRing IsActive="{x:Bind ViewModel.AiServices.IsSelfTesting, Mode=OneWay}" Width="16" Height="16" />
                            <Button x:Uid="AiHub_RunSelfTestButton" Content="Run self-test"
                                    AutomationProperties.AutomationId="AIHub_RunSelfTestButton"
                                    Command="{x:Bind ViewModel.AiServices.SelfTestCommand}" />
                        </StackPanel>
                    </tkcontrols:SettingsCard>

                    <TextBlock Text="{x:Bind ViewModel.AiServices.SelfTestStatusText, Mode=OneWay}"
                               Margin="0,-4,0,0" FontSize="12" TextWrapping="Wrap"
                               Visibility="{x:Bind ViewModel.AiServices.IsSelfTesting, Mode=OneWay}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    
                    <!-- 2. Endpoints: Main Endpoint (SettingsExpander) -->
                    <tkcontrols:SettingsExpander
                        x:Name="AiMainEndpointExpander"
                        x:Uid="AiHub_MainEndpointExpander"
                        Header="Main endpoint"
                        IsExpanded="False">
                        <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind ViewModel.AiServices.MainEndpoint.ModelSummary, Mode=OneWay}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       VerticalAlignment="Center" />
                            <Button x:Uid="AiHub_TestConnectionButton" Content="Test connection" Command="{x:Bind ViewModel.AiServices.MainEndpoint.TestCommand}" />
                        </StackPanel>
                        <tkcontrols:SettingsExpander.Items>
                            <tkcontrols:SettingsCard x:Uid="AiHub_MainEndpoint_UrlCard" Header="Endpoint URL">
                                <TextBox Width="320" Text="{x:Bind ViewModel.AiServices.MainEndpoint.BaseUrl, Mode=TwoWay}" PlaceholderText="https://api.example.com" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard x:Uid="AiHub_MainEndpoint_ApiKeyCard" Header="API key">
                                <PasswordBox x:Name="AiMainApiKeyBox" x:Uid="AiHub_MainEndpoint_ApiKeyBox" Width="320" Password="{x:Bind ViewModel.AiServices.MainEndpoint.ApiKey, Mode=OneWay}" PasswordChanged="AiMainApiKeyBox_PasswordChanged" PlaceholderText="Enter API key" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard x:Uid="AiHub_MainEndpoint_ApiModeCard" Header="API mode">
                                <ComboBox MinWidth="200" SelectedValue="{x:Bind ViewModel.AiServices.MainEndpoint.Mode, Mode=TwoWay}" SelectedValuePath="Tag">
                                    <ComboBoxItem Content="responses (/v1/responses)" Tag="responses" />
                                    <ComboBoxItem Content="chat (/v1/chat/completions)" Tag="chat" />
                                </ComboBox>
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard x:Uid="AiHub_MainEndpoint_ModelCard" Header="Model">
                                <TextBox Width="200" Text="{x:Bind ViewModel.AiServices.MainEndpoint.Model, Mode=TwoWay}" PlaceholderText="gpt-5.6-luna" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard x:Uid="AiHub_MainEndpoint_ReasoningCard" Header="Reasoning effort">
                                <ComboBox MinWidth="200" SelectedValue="{x:Bind ViewModel.AiServices.MainEndpoint.Effort, Mode=TwoWay}" SelectedValuePath="Tag">
                                    <ComboBoxItem Content="low" Tag="low" />
                                    <ComboBoxItem Content="medium" Tag="medium" />
                                    <ComboBoxItem Content="high" Tag="high" />
                                    <ComboBoxItem Content="xhigh" Tag="xhigh" />
                                    <ComboBoxItem Content="max" Tag="max" />
                                </ComboBox>
                            </tkcontrols:SettingsCard>
                        </tkcontrols:SettingsExpander.Items>
                    </tkcontrols:SettingsExpander>
    
                    <!-- Fallback Endpoint (SettingsExpander) -->
                    <tkcontrols:SettingsExpander
                        x:Name="AiFallbackEndpointExpander"
                        x:Uid="AiHub_FallbackEndpointExpander"
                        Header="Fallback endpoint"
                        IsExpanded="False">
                        <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind ViewModel.AiServices.FallbackEndpoint.ModelSummary, Mode=OneWay}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       VerticalAlignment="Center" />
                            <Button x:Uid="AiHub_TestConnectionButton" Content="Test connection" Command="{x:Bind ViewModel.AiServices.FallbackEndpoint.TestCommand}" />
                        </StackPanel>
                        <tkcontrols:SettingsExpander.Items>
                            <tkcontrols:SettingsCard x:Uid="AiHub_MainEndpoint_UrlCard" Header="Endpoint URL">
                                <TextBox Width="320" Text="{x:Bind ViewModel.AiServices.FallbackEndpoint.BaseUrl, Mode=TwoWay}" PlaceholderText="https://api.example.com" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard x:Uid="AiHub_MainEndpoint_ApiKeyCard" Header="API key">
                                <PasswordBox x:Name="AiFallbackApiKeyBox" Width="320" Password="{x:Bind ViewModel.AiServices.FallbackEndpoint.ApiKey, Mode=OneWay}" PasswordChanged="AiFallbackApiKeyBox_PasswordChanged" PlaceholderText="Enter API key" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard x:Uid="AiHub_MainEndpoint_ApiModeCard" Header="API mode">
                                <ComboBox MinWidth="200" SelectedValue="{x:Bind ViewModel.AiServices.FallbackEndpoint.Mode, Mode=TwoWay}" SelectedValuePath="Tag">
                                    <ComboBoxItem Content="responses (/v1/responses)" Tag="responses" />
                                    <ComboBoxItem Content="chat (/v1/chat/completions)" Tag="chat" />
                                </ComboBox>
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard x:Uid="AiHub_MainEndpoint_ModelCard" Header="Model">
                                <TextBox Width="200" Text="{x:Bind ViewModel.AiServices.FallbackEndpoint.Model, Mode=TwoWay}" PlaceholderText="gpt-5.6-luna" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard x:Uid="AiHub_MainEndpoint_ReasoningCard" Header="Reasoning effort">
                                <ComboBox MinWidth="200" SelectedValue="{x:Bind ViewModel.AiServices.FallbackEndpoint.Effort, Mode=TwoWay}" SelectedValuePath="Tag">
                                    <ComboBoxItem Content="low" Tag="low" />
                                    <ComboBoxItem Content="medium" Tag="medium" />
                                    <ComboBoxItem Content="high" Tag="high" />
                                    <ComboBoxItem Content="xhigh" Tag="xhigh" />
                                    <ComboBoxItem Content="max" Tag="max" />
                                </ComboBox>
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard x:Uid="AiHub_ClearFallbackCard" Header="Clear fallback endpoint">
                                <Button x:Uid="AiHub_ClearFallbackButton" Content="Clear" Command="{x:Bind ViewModel.AiServices.ClearFallbackCommand}" />
                            </tkcontrols:SettingsCard>
                        </tkcontrols:SettingsExpander.Items>
                    </tkcontrols:SettingsExpander>
    
                    <!-- 4. Policy rules (SettingsExpander) -->
                    <tkcontrols:SettingsExpander
                        x:Name="AiPolicyRulesExpander"
                        x:Uid="AiHub_PolicyRulesExpander"
                        Header="Policy rules"
                        Description="Global security policy and task instructions (security.md and AGENTS.md)"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE946;}"
                        IsExpanded="False">
                        <tkcontrols:SettingsExpander.Items>
                            <tkcontrols:SettingsCard ContentAlignment="Vertical" HorizontalContentAlignment="Stretch">
                                <StackPanel Spacing="12" Margin="0,4,0,4">
                                    <!-- The global policy governs every plugin, so it stands
                                         alone here; task-specific chains live on AI Hub. -->
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE72E;" FontSize="14" VerticalAlignment="Center" />
                                        <TextBlock Text="Global security policy"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" />
                                    </StackPanel>
    
                                    <!-- Code/Policy Content Editor -->
                                    <TextBox
                                        x:Name="AiPolicyContentBox"
                                        Text="{x:Bind ViewModel.AiServices.CurrentPolicyContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        AcceptsReturn="True"
                                        TextWrapping="Wrap"
                                        MinHeight="160"
                                        MaxHeight="320"
                                        MaxLength="64000"
                                        FontFamily="Consolas, Cascadia Code"
                                        FontSize="12"
                                        ScrollViewer.VerticalScrollBarVisibility="Auto" />
    
                                    <!-- Bottom action row with target file path indicator & buttons -->
                                    <Grid ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
    
                                        <TextBlock
                                            Grid.Column="0"
                                            Text="{x:Bind ViewModel.AiServices.CurrentPolicyFilePath, Mode=OneWay}"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            VerticalAlignment="Center"
                                            TextTrimming="CharacterEllipsis" />
    
                                        <Button
                                            Grid.Column="1"
                                            x:Uid="AiHub_ResetPolicyButton"
                                            Content="Reset to default"
                                            Command="{x:Bind ViewModel.AiServices.ResetActivePolicyCommand}" />
    
                                        <Button
                                            Grid.Column="2"
                                            x:Uid="AiHub_SavePolicyButton"
                                            Content="Save"
                                            Style="{StaticResource AccentButtonStyle}"
                                            Command="{x:Bind ViewModel.AiServices.SaveActivePolicyCommand}" />
                                    </Grid>
                                </StackPanel>
                            </tkcontrols:SettingsCard>
                        </tkcontrols:SettingsExpander.Items>
                    </tkcontrols:SettingsExpander>
                        </StackPanel>
                    </ContentControl>

                </controls:SettingsGroup>
                <controls:SettingsGroup x:Uid="General_Experimentation" Visibility="Visible">
                    <controls:GPOInfoControl ShowWarning="{x:Bind ViewModel.IsExperimentationGpoDisallowed, Mode=OneWay}">
                        <tkcontrols:SettingsCard x:Name="GeneralPageEnableExperimentation" x:Uid="GeneralPage_EnableExperimentation">
                            <tkcontrols:SettingsCard.HeaderIcon>
                                <PathIcon Data="M1859 1758q14 23 21 47t7 51q0 40-15 75t-41 61-61 41-75 15H354q-40 0-75-15t-61-41-41-61-15-75q0-27 6-51t21-47l569-992q10-14 10-34V128H640V0h768v128h-128v604q0 19 10 35l569 991zM896 732q0 53-27 99l-331 577h972l-331-577q-27-46-27-99V128H896v604zm799 1188q26 0 44-19t19-45q0-10-2-17t-8-16l-164-287H464l-165 287q-9 15-9 33 0 26 18 45t46 19h1341z" />
                            </tkcontrols:SettingsCard.HeaderIcon>
                            <ToggleSwitch
                                x:Uid="ToggleSwitch"
                                AutomationProperties.Name="{Binding ElementName=GeneralPageEnableExperimentation, Path=Header}"
                                IsOn="{x:Bind ViewModel.EnableExperimentation, Mode=TwoWay}" />
                        </tkcontrols:SettingsCard>
                    </controls:GPOInfoControl>
                </controls:SettingsGroup>

                <controls:SettingsGroup x:Uid="General_SettingsBackupAndRestoreTitle" Visibility="Visible">
                    <tkcontrols:SettingsExpander
                        x:Name="GeneralSettingsBackupAndRestore"
                        x:Uid="General_SettingsBackupAndRestore"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE777;}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Uid="General_SettingsBackupAndRestore_ButtonBackup" Command="{Binding BackupConfigsEventHandler}" />
                            <Button x:Uid="General_SettingsBackupAndRestore_ButtonRestore" Command="{Binding RestoreConfigsEventHandler}" />
                        </StackPanel>
                        <tkcontrols:SettingsExpander.Items>
                            <tkcontrols:SettingsCard x:Name="GeneralSettingsBackupAndRestoreLocationText" x:Uid="General_SettingsBackupAndRestoreLocationText">
                                <Grid ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        x:Name="pathTextBlock"
                                        MaxWidth="286"
                                        VerticalAlignment="Center"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        IsTextSelectionEnabled="True"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Text="{x:Bind ViewModel.SettingsBackupAndRestoreDir, Mode=TwoWay}"
                                        TextWrapping="Wrap">
                                        <ToolTipService.ToolTip>
                                            <ToolTip IsEnabled="{Binding IsTextTrimmed, ElementName=pathTextBlock, Mode=OneWay}">
                                                <TextBlock Text="{x:Bind ViewModel.SettingsBackupAndRestoreDir, Mode=TwoWay}" />
                                            </ToolTip>
                                        </ToolTipService.ToolTip>
                                    </TextBlock>
                                    <Button
                                        x:Uid="General_SettingsBackupAndRestore_ButtonSelectFolder"
                                        Grid.Column="1"
                                        Command="{Binding SelectSettingBackupDirEventHandler}"
                                        Content="&#xe8da;"
                                        FontFamily="{ThemeResource SymbolThemeFontFamily}">
                                        <ToolTipService.ToolTip>
                                            <ToolTip>
                                                <TextBlock x:Uid="General_SettingsBackupAndRestore_ButtonSelectLocation" />
                                            </ToolTip>
                                        </ToolTipService.ToolTip>
                                    </Button>
                                </Grid>
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard
                                x:Name="GeneralSettingsBackupAndRestoreStatusInfo"
                                x:Uid="General_SettingsBackupAndRestoreStatusInfo"
                                HorizontalContentAlignment="Left"
                                ContentAlignment="Vertical">
                                <Grid
                                    Margin="0,0,0,6"
                                    ColumnSpacing="36"
                                    RowSpacing="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock x:Uid="General_SettingsBackupInfo_StatusHeader" />
                                    <TextBlock
                                        Grid.Column="1"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        TextWrapping="WrapWholeWords">
                                        <Run Text="{x:Bind ViewModel.CurrentSettingMatchText, Mode=OneWay}" />
                                        <Hyperlink Click="UpdateBackupAndRestoreStatusText" TextDecorations="Underline">
                                            <Run x:Uid="General_SettingsBackupAndRestore_LinkRefresh" />
                                        </Hyperlink>
                                    </TextBlock>

                                    <TextBlock x:Uid="General_SettingsBackupInfo_FileNameHeader" Grid.Row="1" />
                                    <TextBlock
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind ViewModel.LastSettingsBackupFileName, Mode=OneWay}"
                                        TextWrapping="WrapWholeWords" />

                                    <TextBlock x:Uid="General_SettingsBackupInfo_SourceHeader" Grid.Row="2" />
                                    <TextBlock
                                        Grid.Row="2"
                                        Grid.Column="1"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind ViewModel.LastSettingsBackupSource, Mode=OneWay}"
                                        TextWrapping="WrapWholeWords" />
                                    <TextBlock x:Uid="General_SettingsBackupInfo_DateHeader" Grid.Row="3" />
                                    <TextBlock
                                        Grid.Row="3"
                                        Grid.Column="1"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind ViewModel.LastSettingsBackupDate, Mode=OneWay}"
                                        TextWrapping="WrapWholeWords" />
                                </Grid>
                            </tkcontrols:SettingsCard>
                        </tkcontrols:SettingsExpander.Items>
                    </tkcontrols:SettingsExpander>
                </controls:SettingsGroup>
                <InfoBar
                    Title="{x:Bind ViewModel.SettingsBackupMessage, Mode=OneWay}"
                    IsClosable="False"
                    IsOpen="{x:Bind ViewModel.SettingsBackupRestoreMessageVisible, Mode=OneWay}"
                    IsTabStop="{x:Bind ViewModel.SettingsBackupRestoreMessageVisible, Mode=OneWay}"
                    Severity="{x:Bind ViewModel.BackupRestoreMessageSeverity, Converter={StaticResource StringToInfoBarSeverityConverter}}" />
            </StackPanel>
        </controls:SettingsPageControl.ModuleContent>
    </controls:SettingsPageControl>
</local:NavigablePage>
